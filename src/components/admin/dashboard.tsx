// COMPONENTS
import { Button, Title } from "../../components/ui";

const Dashboard = () => {
  return (
    <div>
      <Title title="Dashboard" />
      <Button text="Modifier les questions" />
    </div>
  );
};

export default Dashboard;
